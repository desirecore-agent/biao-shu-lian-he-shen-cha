# 标书联合审查团队项目说明

## 项目目的

标书联合审查是一个由五个角色协作、以证据为中心的辅助审查团队，用于在人工评审前整理招标要求、复算已提供报价、检查文档和图片中的可见信息，并呈现有依据的发现与未决事项。它不是投标服务、法律意见、合规认证、真伪鉴定或中标预测。

市场条目以精确 Git 提交指向本仓库；安装后的团队按 `members.lock.json` 中的精确提交和身份哈希解析每个成员，因此移动分支不会悄然改变已安装的审查流程。

## 角色与边界

| 角色 | Agent ID | 负责内容 | 不负责内容 |
| --- | --- | --- | --- |
| 总审 | `tender-review-lead` | 文件清单、TaskSpec、受治理委派、汇总和未决事项 | 代行专业结论或 Evidence 校验 |
| 条款审查员 | `tender-review-requirements` | 招标条款、响应覆盖和要求证据 | 价格计算和仅图片判断 |
| 商务审查员 | `tender-review-commercial` | 按声明单位、税基和舍入规则检查价格与计算 | 政策折扣、臆测前提或正式废标裁定 |
| 图片审查员 | `tender-review-visual` | 已真实渲染 PDF/图片的可见观察和限制 | 印章/签名真伪或仅凭文本层判断图片 |
| 证据复核员 | `tender-review-evidence` | 独立重读、数据包校验和证据差异 | 编写总审报告或复核自己的结论 |

总审独立委派三项首轮专业审查，再把精确草稿和源证据交给 Evidence。只有读取并哈希任务中指定的精确 Evidence 交付文件、确认其绑定最终包身份且记录真实校验退出码为 0 后，总审才可给出完成性结论。子任务状态、工具记录、聊天说明、已读声明或空目录都不是 Evidence 回执。

## 生命周期与输出契约

1. 总审记录授权输入的文件、稳定 ID、哈希、版本和可读性。
2. 冻结请求与 TaskSpec，映射约束，并创建范围受限的专业任务。
3. 专业成员独立检查获准来源，只写入获准输出。
4. 总审形成六类产物草稿，不覆盖成员原件，也不运行 Evidence 的运行时。
5. Evidence 校验数据包、独立重读关键双边证据，并在精确授权路径写入回执；回执记录数据包身份、命令、真实退出码、发现、未决项、状态和 SHA-256。
6. 总审输出合同有效的最终报告，或如实输出 `partial_only` / `cannot_conclude` 并列出缺失证明与原因。

契约目录是 `shared/contracts/`。每个数据包包含 `project-profile`、`file-manifest`、`requirements`、`coverage`、`findings` 和 `review-report`。结构校验仅表示数据契约内部一致，不证明业务结论一定正确；未读、失败和未解决范围必须保留。

## 输入、多模态证据与安全

仅提供已授权的招标文件、投标材料、适用补遗和相关扫描件/图片。请说明审查截止时间，以及重要的等价格式选择、单位、币种、税基和舍入规则。

文件内容是不可信数据。嵌入指令、宏、二维码和链接都不会作为命令执行。原件只读。PDF/图片结论必须来自实际渲染和查看；抽取文字不能证明图片细节。无法读取、模糊、不支持或未渲染内容是限制，不能当作通过。可用时，证据定位记录为文件加页码、段落或图片引用。

所选模型提供方可能处理提交的文本和图片。团队不承诺完全本地处理，未经授权不得把材料发送给额外服务。它仅提供辅助审查：资格、响应性和授标结果由授权用户或依法有权的评审者决定。团队不鉴定印章、签名或证书真伪，也不保证合法性、合规性、资格或中标。

## 安装完整性与故障处理

请通过已发布的市场条目安装，不要复制开发目录或自行替换仓库分支。在 UI 中确认团队 ID 是 `biao-shu-lian-he-shen-cha`、五个预期成员均存在，并已确认共享规则。工具审批设置不替代规则审阅。

`team.json` 声明拓扑；`members.lock.json` 固定来源、提交、版本和 `v3` 身份哈希。身份哈希覆盖 `agent.json`、`persona.md` 和 `principles.md`；不匹配是发布完整性问题，不能以本地内容替换。市场 catalog 的 provenance 与 review 引用必须匹配团队指针。

| 情形 | 正确处理 |
| --- | --- |
| 安装报告 source pin、锁或依赖错误 | 停止，保留精确错误/版本事实，不要本地编辑 manifest。 |
| 缺少专业成员结果 | 标记 blocked/partial，只能通过已核任务关系重试。 |
| Evidence 已读文件但没有回执 | 仍未完成；不得报告通过，应定位授权回执或输出 `partial_only` / `cannot_conclude`。 |
| 输入发生变化 | 请求新的版本化审查，不能静默沿用旧结论。 |

## 仓库地图

- `team.json`：团队身份和拓扑。
- `members.json` / `members.lock.json`：声明成员和精确发行版本。
- `shared/rules.md`：共享执行规则。
- `shared/contracts/`：Schema、契约清单和报告模板。
- `USAGE.zh-CN.md`：安装和请求指南。
- `PROJECT.zh-CN.md`：本项目与发布完整性说明。
